  # Extraia o número do lote do nome do arquivo
    lote_number = os.path.basename(input_file).split('_')[-1].replace('.7z', '')

 # Defina o nome do arquivo de saída de acordo com o novo padrão
    output_file = os.path.join(output_dir, f'FATURA_COBRANCA_ATRAZOCURTO_I_{lote_number}.zip')
    
