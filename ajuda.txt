  // Usando Optional e Stream para obter o primeiro número de contrato vinculado
        Optional<String> primeiroNumeroContrato = Optional.ofNullable(contratoDTO)
            .map(ContratoDTO::getNumeroContratosVinculados)
            .flatMap(lista -> lista.stream().findFirst());
